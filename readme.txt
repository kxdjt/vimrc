0.wget https://raw.githubusercontent.com/kxdjt/vimrc/master/.vimrc -O $HOME/.vimrc
1.Vundle安装：
  a. $mkdir ~/.vim/bundle/ 
  b. $cd ~/.vim/bundle/ 
  c. git clone https://github.com/gmarik/vundle.git ~/.vim/bundle/Vundle.vim 
2. 打开vim 执行:PluginInstall
3.Youcompleteme需要编译，编译方式见配置文件
